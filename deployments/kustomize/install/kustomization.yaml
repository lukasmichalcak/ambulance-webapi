apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml

configMapGenerator:
  - name: lm1234-ambulance-webapi-mongodb-init
    files:
      - params/init-db.js
  - name: lm1234-ambulance-webapi-config
    literals:
      - database=lm1234-ambulance
      - collection=ambulance
patches:
- path: patches/webapi.deployment.yaml
  target:
    group: apps
    version: v1
    kind: Deployment
    name: lm1234-ambulance-webapi